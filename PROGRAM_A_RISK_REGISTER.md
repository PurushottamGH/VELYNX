# PROGRAM A — RISK REGISTER

**Authority:** Chief Scientist / Chief Systems Architect design review,
2026-07-07. Severity: CRITICAL (invalidates EXP-1 or violates canon) > HIGH
(blocks execution or a preregistered kill fires spuriously) > MEDIUM > LOW.
Status: OPEN unless stated. Tags on the *claim* underlying each risk.

| ID | Sev | Risk | Evidence / citation | Mitigation (existing or required) |
|---|---|---|---|---|
| R-01 | CRITICAL | **Tier category error** — retrieval relevance (`RetrievalSource.score`) rebadged as answer confidence. | **[FACT]** prereg §5; `PROGRAM_A_ADAPTER_AUDIT.md` F-3, F-7; BINDING_SPEC R1 | Preregistered kill; forbidden edge in `PROGRAM_A_DEPENDENCY_GRAPH.md` §4; emission surface must not read `score` toward the tier. |
| R-02 | CRITICAL | **Post-hoc tier remapping** — mapping the synthesizer's 5-grade label to the 4-tier set inside the binding. | **[FACT]** prereg §4 kill #5; F-4; BINDING_SPEC R2 | Kill criterion; `from_mapping` rejects the 5-grade taxonomy (tested); binding is pure transport. |
| R-03 | CRITICAL | **Post-hoc calibration** — binning a raw confidence float, or fitting the tier rule to EXP-1 outcomes. | **[FACT]** prereg §6 "Prohibited"; BINDING_SPEC R3 | Kill criterion; tier mechanism must be committed+frozen before outputs (roadmap T1/T2 gates). |
| R-04 | CRITICAL | **[REJECTED]-mechanism binding (TD-08)** — AnswerSynthesizer/ReasoningTrace/thermodynamic_state/kg entering the live EXP-1 path. | **[FACT]** canon `:90`; F-5; BINDING_SPEC R4 | Deny-list (`PROGRAM_A_DEPENDENCY_GRAPH.md` §4); **required:** import-guard test (none exists — TD-08 `[RISK]`). |
| R-05 | CRITICAL | **Dataset leakage** — frozen rows used to tune retrieval, tiering, prompts, or refusal behavior. | **[FACT]** `EXP1_DATASET_SPEC.md` §12.2-12.3 (invalidates the frozen version) | Leakage checks + `development_exclusion_checked` metadata at freeze; held-out discipline after freeze. |
| R-06 | HIGH | **Nondeterministic retrieval** — live web, wall-clock, env-keyed sources break per-(query,seed) determinism and re-run replay. | **[FACT]** `unified_retriever.py:31,156,346,362`; BINDING_SPEC §4 determinism requirement | **[OPEN QUESTION Q2]** frozen retrieval snapshot vs live mode; must be resolved and recorded before freeze. Prereg §7 tolerates seed-level stochasticity but replay does not. |
| R-07 | HIGH | **F-10 tier-validation bypass** — direct-constructed `AnswerRecord` with non-canonical tier propagates to a downstream `KeyError`. | **[FACT]** `PROGRAM_A_ADAPTER_AUDIT.md` §7; documented test; NOT fixed | Interim: binding returns mappings / uses `from_mapping`. Fix (validate in `__post_init__` or re-validate in `_coerce_answer_record`) requires Reviewer + ScientificAuditor sign-off (roadmap T6). |
| R-08 | HIGH | **Specialist unavailability** — Architect/ScientificAuditor/Builder/Reviewer agents returned no output; location + tier-lawfulness decisions cannot be formally ratified. | **[FACT]** BINDING_SPEC R8; `PROGRAM_A_ADAPTER_AUDIT.md` header; `EXP1_TRACE.md` provisional Goodhart verdict | All provisional verdicts are labeled; no pass claim without real sign-off; roadmap gates G1-G3 block on it. |
| R-09 | HIGH | **Missing preregistered artifacts** — a run today emits no `reliability.csv` / `calibration_report.md` / `experiment_summary.json` / `.spec.json`. | **[FACT]** TD-05; `run.py:16-36` omits report/artifact_specs; `EXECUTION_GUIDE.md:64-76,121` | Wire `report.py` + `artifact_specs.py` into the runner before first execution (roadmap T5). |
| R-10 | HIGH | **Degenerate tier use** — a conservative implementation emitting one tier everywhere kills EXP-1 (criterion #3) even with low ECE. | **[FACT]** prereg §4; `decision.py` DEGENERATE_TIER_THRESHOLD=2 | Design-time awareness only; must NOT be "mitigated" by tuning on frozen rows (that is R-05/R-03). |
| R-11 | HIGH | **CERTAIN-on-fabrication recurrence** — the historical Q6 failure mode; zero tolerance. | **[FACT]** canon `:146`; prereg §4 hard hallucination-honesty; `decision.py` tolerance=0 | The emission mechanism's refusal path is load-bearing; EXP-0 (paraphrase precondition) is scheduled per Research Director condition 3 (`RESEARCH_DIRECTOR_DECISION.md` §8). |
| R-12 | MEDIUM | **Goodhart-guard dispute** — passive locked-bins + `protocol_violation` flag vs `STATISTICAL_AUDIT.md:188` demand for an active detector (EXP1-05). | **[FACT]** `EXP1_TRACE.md` ML-14 (DEFERRED + RECORDED DISSENT, provisional) | ScientificAuditor ratification required; if active detector ruled canonical, it becomes a MISSING link before pass claims. |
| R-13 | MEDIUM | **Seed-power mismatch** — if the pipeline is fully deterministic, all 22 replicates are identical and the §7 power precheck's assumptions fail in the conservative direction; prereg's own text requires amendment if seed-level effect <10% is implied. | **[FACT]** prereg §7 last sentence | Resolve with Q2/Q3 before freeze; document the seed-variance source in the mechanism spec. |
| R-14 | MEDIUM | **Documentation fiction** — `ARCHITECTURE_MAP.md:54` (program_a "LIVE"), `repository_v2.md` structure claims; planning against them builds on nonexistent files. | **[FACT]** F-2; TD-03; TD-13 | Corrections tracked (BINDING_SPEC open item 5); this review used disk state only. |
| R-15 | MEDIUM | **BM25 control absent** — canon §6-H1 names a BM25/TF-IDF random-confidence control; EXP1-04 is deferred backlog, not preregistered in §4 kills. | **[FACT]** `EXP1_TRACE.md` ML-13; canon `:108` | Roadmap item post-gate; ScientificAuditor should rule whether the H₀ control is required for the *pass claim* or only for publication. |
| R-16 | LOW | **Packaging** — `program_a/` not in `pyproject.toml` packages.find; placing the surface there without a packaging change makes it uninstallable. | **[FACT]** `pyproject.toml:20-28` (via DEPENDENCY_GRAPH.md §1) | Fold into Architect location decision (Q4). |
| R-17 | LOW | **Repo-root DB files** — `velynx_*.db` at root; accidental commit/interference hazard, unrelated to the EXP-1 path but adjacent to any product persistence. | **[FACT]** TD-15 | `.gitignore` per commit `326cf17`; keep EXP-1 path DB-free (side-effect-free rule). |
