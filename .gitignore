node_modules/
dist/
.env
.vscode/
.DS_Store
coverage/
.openclaude/
.pytest_cache/
__pycache__/
*.py[cod]
.venv/
.venv314/
backend_backup_phase35/
/data/chroma_db/
/models/
/velynx_output/

# Throwaway databases from VELYNX_TEST_MODE test isolation
backend/tests/data/_isolated/
nul

# Runtime databases
*.db
*.sqlite3
backend/data/neural_links.json

# Third-party nested repos (not part of this repository)
kiro-gateway/
searxng/

# Experiment artifacts (code-derived, not committed)
artifacts/experiments/
artifacts/benchmarks/
artifacts/output/
artifacts/publication/

# ChromaDB vector store
**/chroma_db/

# Python egg info
*.egg-info/
*.egg

# IDE settings
.idea/
*.swp
*.swo

# OS files
Thumbs.db
Desktop.ini

# Research artifacts (derived data, not source)
research_artifacts/
!/p1/records/research_artifacts/
!/p1/records/research_artifacts/**

# SQLite WAL/SHM journal files (generated at runtime, not portable)
*.db-shm
*.db-wal

# Local database directory, secrets, and worktrees
.velynx_data/
gcp-key.json.json
P1.worktrees/
*.worktrees/
worktrees/
evidence/simulations/

# Raw per-step experiment dumps (large; kept out of git, sha256 recorded in
# evidence/12_m_recomputation.md provenance table). Summaries + configs +
# recompute_m_hungarian.py are committed and reproduce all reported statistics.
evidence/experiment_logs/**/aggregated_results.json

# P1 Research OS — generated/transient state (see REPOSITORY_CONSTITUTION.md)
/p1/derived/
/p1/.transactions/active/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
